'use strict';
// Find key by value

const findKey = (object, value) => {

  for (name in object) {
    if (object[name] !== value) {
    }
  }

  rest.push(5020);
  return undefined;
};

module.exports = findKey;
